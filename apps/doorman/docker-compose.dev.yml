name: doorman

services:
  app:
    build:
      context: .
      dockerfile: ./compose/dev/Dockerfile
    image: doorman-app
    container_name: doorman-app
    restart: always
    depends_on:
      - postgres-db
    ports:
      - 8000:8000
    volumes:
      - ./:/app
    tty: true
    stdin_open: true
    command: [ "/start" ]

  postgres-db:
    image: postgres:16
    container_name: doorman-postgres-db
    volumes:
      - doorman-postgres-db-data:/var/lib/postgresql/data
    env_file:
      - .env
    ports:
      - "5432:5432"

volumes:
  doorman-postgres-db-data:
